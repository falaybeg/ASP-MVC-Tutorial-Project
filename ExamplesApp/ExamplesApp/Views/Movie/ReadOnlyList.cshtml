@model IEnumerable<ExamplesApp.Models.Movie>
@{
    ViewBag.Title = "Index";
}

<h2>Movies</h2>
@*
                ----*** Here we deleted New Button because user is not Admin user ***----
    


    <table id="movie" class="table table-hover">
        <thead>
            <tr>
                <th>Movies</th>
                <th>Genre</th>
            </tr>
        </thead>
        <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.ActionLink(item.Name, "Details", "Movie", new { id = item.Id }, null)</td>
                        <td>@item.Genre.Name</td>
                        <td>
                            @Html.ActionLink("Edit", "EditMovie", "Movie", new { id = item.Id }, new { @class = "btn btn-warning" })
                                            <button data-movie-id="@item.Id" class="btn btn-danger" id="js-delete">Delete</button>
                                        </td>
                    </tr>
                }
        </tbody>
    </table>
*@

<table id="movies" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Movie</th>
            <th>Genre</th>
            
        </tr>
    </thead>
    <tbody></tbody>
</table>
@section scripts
{
    <script>
        $(document).ready(function () {
            var table = $("#movies").DataTable({
                ajax: {
                    url: "/api/MovieApi",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name"
                    },
                    {
                        data: "genre.name"
                    }
                ]
            });


            @*
               ---*** We deleted Delete Button because user is not Admin user ***---    
            *@
        });
    </script>
}