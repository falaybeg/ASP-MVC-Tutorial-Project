@model ExamplesApp.Models.Customer

<h2>@Model.Name</h2>

<ul>
    <li><b>Membership Type:</b>   @Model.MemberShipType.Name</li>
    @if (Model.Birthdate.HasValue)
    {
        <li><b>BirthDate:</b>   @Model.Birthdate.Value.ToShortDateString()</li>
    }
</ul>